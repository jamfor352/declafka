kafka:
  default:
    bootstrap.servers: "localhost:39092"
    auto.offset.reset: "earliest"
    security.protocol: "PLAINTEXT"
  test-listener-2:
    group.id: "test-listener-group"
  dlq-listener:
    group.id: "dlq-listener-group"
